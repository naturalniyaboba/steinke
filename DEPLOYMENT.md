# 📖 Подробная инструкция по публикации сайта на GitHub Pages

## 🎯 Что нужно сделать

Эта инструкция поможет вам опубликовать ваш сайт Steinke Immobilien в открытом доступе через GitHub Pages.

---

## 📋 Шаг 1: Подготовка репозитория на GitHub

### 1.1. Убедитесь, что код загружен на GitHub

1. Откройте терминал в папке проекта
2. Проверьте статус репозитория:
   ```bash
   git status
   ```
3. Если есть несохраненные изменения, добавьте их:
   ```bash
   git add .
   git commit -m "Настройка для GitHub Pages"
   ```
4. Отправьте изменения на GitHub:
   ```bash
   git push origin main
   ```
   (или `git push origin master`, если ваша основная ветка называется `master`)

---

## 🔧 Шаг 2: Настройка GitHub Pages

### 2.1. Откройте настройки репозитория

1. Перейдите на GitHub.com
2. Откройте ваш репозиторий `steinke`
3. Нажмите на вкладку **"Settings"** (Настройки) в верхнем меню репозитория

### 2.2. Включите GitHub Pages

1. В левом меню найдите раздел **"Pages"** (или **"Страницы"**)
2. В разделе **"Source"** (Источник) выберите:
   - **"GitHub Actions"** (НЕ "Deploy from a branch"!)
3. Сохраните изменения

> ⚠️ **Важно**: Выберите именно "GitHub Actions", а не "Deploy from a branch", так как мы настроили автоматический деплой через Actions.

---

## 🚀 Шаг 3: Запуск деплоя

### 3.1. Автоматический деплой

После того, как вы включили GitHub Pages и запушили код:

1. Перейдите во вкладку **"Actions"** в вашем репозитории
2. Вы увидите workflow с названием **"Deploy to GitHub Pages"**
3. Нажмите на него, чтобы увидеть процесс сборки
4. Дождитесь завершения (обычно 2-5 минут)

### 3.2. Проверка статуса деплоя

- ✅ **Желтый кружок** = процесс идет
- ✅ **Зеленая галочка** = успешно завершено
- ❌ **Красный крестик** = ошибка (нажмите на workflow, чтобы увидеть детали)

---

## 🌐 Шаг 4: Получение ссылки на сайт

### 4.1. URL вашего сайта

После успешного деплоя ваш сайт будет доступен по адресу:

```
https://ВАШЕ_ИМЯ_ПОЛЬЗОВАТЕЛЯ.github.io/steinke/
```

Например:
- Если ваш GitHub username `ivanov`, то URL: `https://ivanov.github.io/steinke/`
- Если ваш GitHub username `petrov`, то URL: `https://petrov.github.io/steinke/`

### 4.2. Где найти ссылку

1. Перейдите в **Settings → Pages**
2. Вверху страницы будет указан URL вашего сайта
3. Или просто подставьте ваше имя пользователя в формулу выше

---

## ⚙️ Шаг 5: Настройка base path (если нужно)

### 5.1. Если репозиторий называется НЕ "steinke"

Если ваш репозиторий имеет другое название, нужно изменить настройку:

1. Откройте файл `vite.config.ts`
2. Найдите строку 8:
   ```typescript
   base: mode === "production" ? "/steinke/" : "/",
   ```
3. Замените `"/steinke/"` на `"/ВАШЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ/"`
4. Сохраните и запушите изменения:
   ```bash
   git add vite.config.ts
   git commit -m "Исправлен base path"
   git push
   ```

### 5.2. Если это user/organization page

Если вы хотите, чтобы сайт был доступен по адресу `username.github.io` (без `/steinke/`):

1. Репозиторий должен называться `username.github.io` (где username - ваше имя пользователя)
2. В `vite.config.ts` измените на:
   ```typescript
   base: mode === "production" ? "/" : "/",
   ```

---

## 🔄 Шаг 6: Обновление сайта

Каждый раз, когда вы хотите обновить сайт:

1. Внесите изменения в код
2. Сохраните файлы
3. Выполните в терминале:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
4. GitHub Actions автоматически пересоберет и задеплоит сайт
5. Подождите 2-5 минут
6. Обновите страницу сайта (Ctrl+F5 для полной перезагрузки)

---

## 🐛 Решение проблем

### Проблема: Белый экран при открытии сайта

**Решение:**
1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что в `vite.config.ts` правильно указан `base` path
3. Проверьте, что в `src/App.tsx` используется `basename={import.meta.env.BASE_URL}`
4. Убедитесь, что GitHub Actions успешно завершился

### Проблема: GitHub Actions не запускается

**Решение:**
1. Проверьте, что файл `.github/workflows/deploy.yml` существует
2. Убедитесь, что вы включили GitHub Pages с источником "GitHub Actions"
3. Проверьте, что вы запушили код в ветку `main` или `master`

### Проблема: Сайт не обновляется

**Решение:**
1. Подождите 5-10 минут (деплой может занять время)
2. Очистите кэш браузера (Ctrl+Shift+Delete)
3. Проверьте статус в разделе Actions на GitHub

### Проблема: Ошибки при сборке

**Решение:**
1. Откройте вкладку Actions на GitHub
2. Нажмите на упавший workflow
3. Посмотрите логи ошибок
4. Убедитесь, что все зависимости установлены (`npm install`)

---

## 📝 Чек-лист перед публикацией

- [ ] Код загружен на GitHub
- [ ] Включен GitHub Pages в настройках репозитория
- [ ] Выбран источник "GitHub Actions"
- [ ] Правильно указан `base` path в `vite.config.ts`
- [ ] GitHub Actions workflow успешно выполнился
- [ ] Сайт открывается по ссылке
- [ ] Все страницы работают (главная, Impressum, Datenschutz, AGB)

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен в интернете по адресу:
`https://ВАШЕ_ИМЯ_ПОЛЬЗОВАТЕЛЯ.github.io/steinke/`

Вы можете поделиться этой ссылкой с кем угодно!

---

## 💡 Дополнительные возможности

### Подключение собственного домена

Если у вас есть собственный домен (например, `steinke-immobilien.de`):

1. В настройках репозитория: **Settings → Pages**
2. В разделе **"Custom domain"** введите ваш домен
3. Настройте DNS записи у вашего регистратора домена
4. Подробнее: [GitHub Pages Custom Domain](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)

### HTTPS

GitHub Pages автоматически предоставляет HTTPS для всех сайтов. Никаких дополнительных настроек не требуется!

---

**Удачи с публикацией! 🚀**

